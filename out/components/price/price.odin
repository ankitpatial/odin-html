// Code generated by ohtml. DO NOT EDIT.
package price

import "core:io"
import "../../rt"

Props :: struct {
    amount: f64,
    sale_amount: f64,
    on_sale: bool,
}

render :: proc(w: io.Writer, props: Props) {
    if props.on_sale {
        io.write_string(w, "<span class=\"price--old\">")
        io.write_string(w, "$")
        rt.html_escape(w, fmt.tprintf("%.2f", props.amount))
        io.write_string(w, "</span>")
        io.write_string(w, "<span class=\"price--sale\">")
        io.write_string(w, "$")
        rt.html_escape(w, fmt.tprintf("%.2f", props.sale_amount))
        io.write_string(w, "</span>")
    } else {
        io.write_string(w, "<span class=\"price\">")
        io.write_string(w, "$")
        rt.html_escape(w, fmt.tprintf("%.2f", props.amount))
        io.write_string(w, "</span>")
    }
}
