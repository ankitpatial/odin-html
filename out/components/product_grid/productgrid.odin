// Code generated by ohtml. DO NOT EDIT.
package product_grid

import "core:io"
import "../../rt"

Product :: struct {
    name: string,
    slug: string,
}

Props :: struct {
    title: string,
    products: []Product,
    item: rt.Snippet(Product),
}

render :: proc(w: io.Writer, props: Props) {
    io.write_string(w, "<section class=\"product-grid\">")
    io.write_string(w, "<h2>")
    rt.html_escape(w, props.title)
    io.write_string(w, "</h2>")
    io.write_string(w, "<div class=\"product-grid__items\">")
    if len(props.products) > 0 {
        for p in props.products {
            rt.snippet_render(Product, w, props.item, p)
        }
    } else {
        io.write_string(w, "<p class=\"empty\">No products found.</p>")
    }
    io.write_string(w, "</div>")
    io.write_string(w, "</section>")
}
