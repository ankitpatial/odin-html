// Code generated by ohtml. DO NOT EDIT.
package products

import "core:io"
import "core:fmt"
import "../../rt"
import "../../components/product_grid"
import "../../components/badge"

Props :: struct {
    category: string,
    products: []product_grid.Product,
}

_product_grid_productgrid_0_item_render :: proc(w: io.Writer, ctx_ptr: rawptr, p: product_grid.Product) {
    io.write_string(w, "<div class=\"product-tile\">")
    io.write_string(w, "<a")
    io.write_string(w, " href=\"")
    rt.html_escape(w, fmt.tprint(p.slug))
    io.write_string(w, "\"")
    io.write_string(w, ">")
    io.write_string(w, "<h3>")
    rt.html_escape(w, fmt.tprint(p.name))
    io.write_string(w, "</h3>")
    io.write_string(w, "</a>")
    badge.render(w, badge.Props{
        label = "View",
        variant = "badge--link",
    })
    io.write_string(w, "</div>")
}


render :: proc(w: io.Writer, props: Props) {
    io.write_string(w, "<!DOCTYPE html>")
    io.write_string(w, "<html lang=\"en\">")
    io.write_string(w, "<head><meta charset=\"utf-8\"/><title>Odin Shop</title></head>")
    io.write_string(w, "<body>")
    io.write_string(w, "<nav class=\"nav\"><a href=\"/\" class=\"nav__logo\">Odin Shop</a><a href=\"/products\">Products</a><a href=\"/cart\">Cart</a><a href=\"/about\">About</a></nav>")
    io.write_string(w, "<main>")
    io.write_string(w, "<h1>")
    rt.html_escape(w, props.category)
    io.write_string(w, "</h1>")
    product_grid.render(w, product_grid.Props{
        title = props.category,
        products = props.products,
        item = rt.Snippet(product_grid.Product){
            func = _product_grid_productgrid_0_item_render,
            ctx = nil,
        },
    })
    io.write_string(w, "</main><footer><p>Odin Shop - Built with ohtml</p></footer>")
    io.write_string(w, "</body>")
    io.write_string(w, "</html>")
}
